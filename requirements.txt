fast-agent-mcp  # Main framework (includes MCP support)
python-dotenv>=1.0.1  # For loading .env files locally
requests>=2.31.0  # For MCP discovery calls
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
